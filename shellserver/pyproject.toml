[project]
name = "shellserver"
version = "0.1.0"
description = "MCP server with a terminal tool for running shell commands"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]>=1.27.2",
]
